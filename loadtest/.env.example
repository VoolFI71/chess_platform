# Скопируйте в .env и подставьте стейдж / локальный gateway.
# k6: k6 run --env-file loadtest/.env loadtest/k6/puzzles-random.js
#     (если ваша версия k6 поддерживает --env-file; иначе задайте переменные вручную)

BASE_URL=https://chessmint.ru
RATING_MIN=800
RATING_MAX=2000
